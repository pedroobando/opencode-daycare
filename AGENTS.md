<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Asking questions

- **`question` tool** → complex decisions or 2–4 well-defined options with real consequences. Put the recommendation first.
- **Plain text** → simple, direct, or open-ended questions (yes/no, confirmations, clarifications). Don't make the user click through a menu for a one-liner.

## Project

- Stack: Next.js 16.3.1 (App Router) + React 19.2.8 + Tailwind CSS v4, TypeScript strict.
- Backend: Supabase (Postgres + Auth + Storage + RLS). Project ref `fshwfkppcetvqnrccllq` — managed via the Supabase MCP. DB schema reference: `../07-DB-Schema/opendaycare-database-schema.md` (project reference `docs/` in `opencode.json`). It is **not yet applied** to the database — only docs.
- Product: `open-daycare` — UI copy in Spanish (see `reference/pantallas/`).
- Currently the unmodified `create-next-app` starter. `app/page.tsx` and `app/layout.tsx` are still the boilerplate.

## Scripts

- `pnpm dev` — `next dev` on http://localhost:3000
- `pnpm build` — `next build`
- `pnpm start` — `next start` (after build)
- `pnpm lint` — ESLint flat config (`eslint-config-next/core-web-vitals` + `typescript`).
- **No `test` or `typecheck` script.** To typecheck run `npx tsc --noEmit` (uses `tsconfig.json`).

## Conventions

- **Path alias:** `@/*` maps to the repo root (`tsconfig.json` → `paths: { "@/*": ["./*"] }`). So `import x from "@/app/foo"` resolves to `./app/foo`, not `./src/app/foo`.
- **Tailwind v4 only:** `app/globals.css` uses `@import "tailwindcss"` + `@theme inline`. Do **not** add v3-style `@tailwind base/components/utilities` directives. PostCSS plugin is `@tailwindcss/postcss`.
- **Next 16 typed routes:** `app/layout.tsx` uses `LayoutProps<"/">`. Generated route types live in `.next/types/**/*.ts` and `.next/dev/types/**/*.ts` (both in `tsconfig.json` `include`).
- **No test framework is configured.** Don't add jest/vitest/playwright-test without asking the user first.

## Clean code conventions

- **Code identifiers in English:** all variables, functions, constants, classes, types, interfaces, enums, files, and folders must be named in English.
- **UI copy stays in Spanish:** only user-facing text (labels, messages, etc.) remains in Spanish; see `reference/pantallas/` and future i18n files.
- **Meaningful names:** choose names that reveal intent; avoid abbreviations, acronyms, or single-letter names except for common cases like loop indices.
- **Naming casing:**
  - `camelCase` for variables, functions, hooks, and instance methods.
  - `PascalCase` for React components, types, interfaces, classes, and enums.
  - `UPPER_SNAKE_CASE` for true constants and environment variable keys.
- **Functions do one thing:** keep functions small and focused on a single responsibility; prefer pure functions and explicit return types when it improves readability.
- **Functions as arrow functions:** all functions in the codebase (helpers, handlers, reusable components, etc.) are declared as arrow functions.
- **Route root components as regular functions:** route root components (`page.tsx`, `layout.tsx`, `not-found.tsx`, etc.) may be declared as regular `function` declarations.
- **Avoid magic values:** extract literals and repeated values into named constants.
- **Comments in English:** all inline comments, JSDoc/TSDoc, and documentation notes must be written in English.
- **Follow existing project rules:** keep TypeScript strict, ESLint flat config, and Tailwind v4 conventions.

## Project layout

- `app/` — entry code (App Router) and server-side mutations.
  - Routes live at the top level (`layout.tsx`, `page.tsx`, `globals.css`, `favicon.ico`, …).
  - `app/actions/` — server actions grouped by domain (see the Server Actions section below).
- `public/` — static assets (`next.svg`, `vercel.svg`, etc.).
- `reference/pantallas/*.dc.html` + `support.js` — visual mockups for the target OpenDayCare product (Spanish). **Docs only; the Next.js app does not import or build them.** `support.js` header explicitly says "do not edit" — it's generated by an external `dc-runtime`.
- `reference/screenshots/*.png` — same mockups as images.
- `specs/` — feature/UI specs created via the `/spec` skill. Numbered sequentially with a zero-padded `NN-` prefix (kebab-case slug): `specs/01-feed-home.md`.
- `specs/dbase/` — Supabase/Postgres specs: DDL, RLS, migrations, Edge Functions, Storage, Realtime, cron, pgvector, etc. Anything the spec needs the Supabase MCP to implement. Numbering is **independent** of `specs/` (restarts at `01-`): `specs/dbase/01-rls-posts.md`. The folder is created when the first DB spec lands.
- `supabase/` — does not exist yet. Will hold migrations, declarative schemas, and config once schema work begins. Approved `specs/dbase/*` specs are implemented here.
- `../07-DB-Schema/opendaycare-database-schema.md` — canonical DB dictionary (project reference `docs/` in `opencode.json`). Reference only; **not yet migrated** to the live database.
- `.playwright-mcp/` — gitignored; Playwright MCP screenshots land here.

## Server Actions

Server Actions live in `app/actions/<domain>/`, with a barrel `index.ts` re-exporting each action. They are the bridge between the UI and Supabase — use them to mutate database data from the UI; reads should happen in Server Components, not in actions. They use `createSupabaseServerClient()` from `lib/supabase/server.ts` and each file starts with `'use server';`. Always authenticate, authorize, and validate inputs inside the action. More server actions still need to be added as features land.

## Skills (installed)

Loaded automatically based on description match — invoke with the `skill` tool when a task fits.

- **`spec`** — spec-driven workflow. Source `.agents/skills/spec/SKILL.md` (+ `template.md`), pinned via `skills-lock.json` to `klerith/fernando-skills`. Feature/UI specs land in `specs/NN-slug.md`; Supabase/Postgres specs land in `specs/dbase/NN-slug.md` (numbering restarts inside `dbase/`). Kebab-case, zero-padded `NN-` prefix. States: `Borrador`, `En revisión`, `Aprobado`, `Implementado`, `Obsoleto`.
  - **`/spec <desc>`** (`disable-model-invocation: true`): 4 phases — Context → Questions (3–5, mandatory) → Write → Save to `specs/NN-slug.md` with state `Borrador`. Never writes code; replies match the prompt's language; objective must fit in one sentence or the feature is split. Seeds `specs/.spec-config.yml` (default `AutoCreateBranch: true`).
- **`spec-impl`** — implements an approved spec. Validates state means `Aprobado`, creates a branch named after the spec, switches to it, and starts implementation step by step with pauses to review diffs.
- **`spec-verify`** — (via `/spec-verify <NN-slug>` command) invokes the `spec-verifier` subagent to validate a spec's acceptance criteria. Reads the spec, checks each acceptance criterion, consults Context7 for Next.js / App Router / Tailwind v4 recommendations, uses Playwright and vision to compare screens against `reference/screenshots/`, runs the technical checks (`pnpm dev`, `pnpm build`, `pnpm lint`, `npx tsc --noEmit`), marks checks in the spec, and reports the final summary.
- **`supabase`** — general Supabase guidance (auth, RLS, CLI, MCP, Edge Functions, Realtime, Storage). Source `.agents/skills/supabase/SKILL.md` (v0.1.x, author Supabase). Load for **any** Supabase task — schema changes, auth issues, migrations, debugging Supabase errors, security audits, etc.
- **`supabase-postgres-best-practices`** — Postgres performance / design rules maintained by Supabase (8 categories, ~30 rule files in `references/`). Source `.agents/skills/supabase-postgres-best-practices/SKILL.md` (v1.1.1, author Supabase). **Load BEFORE writing or changing anything in Postgres**: tables/columns, schema design, migrations, declarative schema files, RLS policies, indexes, triggers, DB functions, queues/pg_cron, pgvector. Also load when diagnosing slow queries, high CPU, timeouts, EXPLAIN plans, connection exhaustion, locking, bloat, or cross-tenant data leaks.

## Backend (Supabase)

Project ref `fshwfkppcetvqnrccllq`, configured via the Supabase MCP. Before touching Supabase code or DB state:

### Client packages (Next.js ↔ Supabase)

Packages used by this app to talk to Supabase from Next.js (install with pnpm, pin exact versions):

- **`@supabase/supabase-js`** — core client library (`createClient`). Base of everything; required.
- **`@supabase/ssr`** — official package for Next.js App Router. Provides:
  - `createBrowserClient` — client components ("use client"), browser-side queries and auth.
  - `createServerClient` — Server Components / Route Handlers / Server Actions; must be wired to `cookies()` from `next/headers` so auth sessions persist via cookies (middleware refreshes them).
  - This replaces the deprecated `@supabase/auth-helpers-nextjs` — never use auth-helpers in this project.

Planned structure once implemented:

- `lib/supabase/client.ts` — `createBrowserClient` factory (browser).
- `lib/supabase/server.ts` — `createServerClient` factory using `await cookies()` from `next/headers` (server).
- `database.types.ts` — generated TypeScript types via `supabase gen types typescript --linked --schema=public`; passed as generic to both factories for typed queries.

Environment variables (publishable key, safe for the browser) — values live in `.env` (gitignored):

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`

- **Verify docs, not training data.** Supabase changes frequently — fetch `https://supabase.com/changelog.md`, scan for `breaking-change` tags relevant to the task, and consult the docs (MCP `search_docs` → `.md` page fetches → web search).
- **Prefer the MCP over the CLI.** Use the configured Supabase MCP tools (`execute_sql`, `apply_migration`, `get_advisors`, etc.) instead of `supabase db query` when possible — the MCP does not require CLI v2.79.0+.
- **Schema workflow.** This project does not yet use declarative schemas (`supabase/schemas/` does not exist). Treat as **imperative migrations**: use `execute_sql` / `apply_migration` for iteration, then commit a clean migration when ready (`supabase db pull <name> --local --yes` after running `get_advisors`).
- **Never expose `service_role` / secret keys in clients.** In Next.js, any `NEXT_PUBLIC_*` env var is sent to the browser — keep server-only secrets out of `NEXT_PUBLIC_*`. Prefer publishable keys for frontend code.
- **Apply the security checklist.** Full list lives in `.agents/skills/supabase/SKILL.md` §6; the non-negotiables for this project:
  - RLS on every table in `public` (and any exposed schema). `auth.role()` is deprecated — use the `TO authenticated` / `TO anon` clause.
  - `TO authenticated` alone is **authentication, not authorization** — always combine with an ownership predicate in `USING` (e.g. `(select auth.uid()) = user_id`) to avoid BOLA / IDOR.
  - `UPDATE` policies need **both** `USING` and `WITH CHECK`; without `WITH CHECK`, a user can reassign `user_id` to another user.
  - `UPDATE` requires a `SELECT` policy — otherwise updates silently return 0 rows.
  - Never use `user_metadata` for authorization — it's user-editable; use `app_metadata` instead.
  - Views bypass RLS by default — use `CREATE VIEW ... WITH (security_invoker = true)` (Postgres 15+).
  - `SECURITY DEFINER` functions bypass RLS and are **publicly callable** in `public` (Postgres grants `EXECUTE` to `PUBLIC` by default). Avoid adding `SECURITY DEFINER` to "fix" permission errors; prefer `SECURITY INVOKER`, or move the function to a non-exposed schema and add an `auth.uid()` check in the body.
  - Storage upsert requires `INSERT + SELECT + UPDATE` grants — `INSERT` alone silently fails on replacement.
- **DB specs live in `specs/dbase/`.** When a spec needs the Supabase MCP (DDL/RLS/Edge Functions/Storage/Realtime/cron/pgvector/…), it goes in `specs/dbase/NN-slug.md` with its own numbering — not in `specs/`. When approved and implemented, the work lands in `supabase/`.
- **Run `get_advisors` (MCP) / `supabase db advisors` (CLI v2.81.3+)** after schema or RLS changes.
- **Pin Supabase package versions** (`supabase-js`, `@supabase/ssr`) and commit lockfiles — see `https://supabase.com/docs/guides/security/npm-security.md`.

## Agents

- **`@spec-verifier`** — Subagente especializado en validar los criterios de aceptación de un spec. Lee el spec, consulta Context7 para recomendaciones de Next.js / App Router / Tailwind v4, usa Playwright y visión para comparar pantallas contra `reference/screenshots/`, y ejecuta los checks técnicos (`pnpm dev`, `pnpm build`, `pnpm lint`, `npx tsc --noEmit`). Se invoca mediante `/spec-verify <NN-slug>`.

## MCPs (configured in `opencode.json`)

- **Playwright** (local, `@playwright/mcp@latest`) — for browser automation and screenshots. Saves files to `.playwright-mcp/`.
- **Context7** (remote, bearer-token auth) — for current library/framework docs (Next.js, React, Tailwind, etc.). Prefer over training-data guesses; your knowledge may be stale.
- **Supabase** (remote, OAuth) — `https://mcp.supabase.com/mcp?project_ref=fshwfkppcetvqnrccllq&features=docs,account,database,debugging,development,functions,branching`. Use for all DB / Auth / Storage / Edge Functions work in this project. Prefer MCP tools (`execute_sql`, `apply_migration`, `get_advisors`, `search_docs`, …) over the Supabase CLI.

## Gotchas

- Both `package-lock.json` and `pnpm-lock.yaml` exist. Use pnpm commands; the lockfile is `pnpm-lock.yaml`.
- `pnpm-workspace.yaml` enables `unrs-resolver` build (`allowBuilds.unrs-resolver: true`). Don't remove this.
- `.next/` is gitignored. Never commit build artifacts.
- The `BEGIN:nextjs-agent-rules` block above is regenerated by `next dev`. Either keep it or commit it cleanly — don't try to suppress it.
- `next-env.d.ts` is generated; do not edit or commit (gitignored).
- No CI, no pre-commit hooks, no Husky. Lint/typecheck discipline is manual.
